services:
  - type: web
    name: livraria-frontend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: API_BASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
